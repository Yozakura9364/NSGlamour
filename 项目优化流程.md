# 存量项目优化流程

## 第一步：先做工程化底座
1. 梳理并补齐项目文档，形成统一入口（如 `AGENTS.md` + 模块文档）。  
2. 统一执行流程：先看文档 → 出计划 → 确认后开发。  
3. 开发中遇到文档未覆盖的字段、接口或流程，必须先确认再继续。  
4. 功能完成后立即更新文档（改了什么、为什么、影响范围、验证结果）。  

## 第二步：优化技术栈
1. 渐进引入 Vite，先并行验证再切换，避免一次性迁移风险。  
2. 为 IndexedDB 增加 Repository 层，把存储细节和业务逻辑分离。  
3. 业务代码只依赖 Repository 接口，不直接操作 IndexedDB。  

## 第三步：优化项目结构
1. 识别高频重复代码，优先抽取稳定的公共方法/公共模块，目录分层：`core / services / stores / renderer`
2. 小步替换，不做大爆炸式重构。  
3. 每次抽取都做回归验证，确保业务行为不变。  
